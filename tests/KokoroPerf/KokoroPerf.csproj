<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Vernacula.KokoroPerf</RootNamespace>
    <AssemblyName>KokoroPerf</AssemblyName>
    <Platforms>x64</Platforms>
  </PropertyGroup>

  <ItemGroup>
    <!-- .Gpu includes the CPU provider as fallback; covers both EP modes. -->
    <PackageReference Include="Microsoft.ML.OnnxRuntime.Gpu" Version="1.24.4" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Chatterbox.Base\Chatterbox.Base.csproj">
      <Private>true</Private>
      <SetConfiguration>EP=Cuda</SetConfiguration>
    </ProjectReference>
  </ItemGroup>

</Project>
