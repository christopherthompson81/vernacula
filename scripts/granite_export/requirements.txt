# Python 3.11 or 3.12 recommended.
#
# Granite Speech 4.1 is Apache-2.0; no `huggingface-cli login` required for
# the base model. GPU export requires a CUDA-capable torch build.

torch>=2.9,<3              # >=2.9 for the dynamo-based torch.onnx.export default
transformers>=4.57         # GraniteSpeechForConditionalGeneration landed in 4.57
accelerate>=1.0,<2         # required by from_pretrained(low_cpu_mem_usage=True)
sentencepiece>=0.2,<1      # Granite tokenizer
onnx>=1.17,<2
onnxscript>=0.6            # required by torch.onnx.export(dynamo=True)
# Install exactly one of these — they conflict if both are present:
#   CPU only:  pip install onnxruntime>=1.20,<2
#   CUDA GPU:  pip install onnxruntime-gpu>=1.20,<2
onnxruntime-gpu>=1.20,<2
huggingface_hub>=0.30,<1
numpy>=1.26,<3
soundfile>=0.12,<1         # for parity smoke tests against fixture clips
librosa>=0.10,<1           # GraniteSpeechFeatureExtractor uses librosa-style mel
