# Requirements for DeepFilterNet3 ONNX export and validation
#
# Use Python 3.10–3.12. Install DeepFilterNet from source or PyPI, then
# install these additional dependencies.
#
# Install DeepFilterNet (provides the `df` Python module and `libdf`):
#   pip install deepfilternet
#
# Or from source (for development builds):
#   pip install -e /path/to/DeepFilterNet/pyDF
#   pip install -e /path/to/DeepFilterNet/pyDF-data
#
torch>=2.2
onnx>=1.17
onnxruntime>=1.16
numpy<2.0
soundfile>=0.12
deepfilternet>=0.5.6
